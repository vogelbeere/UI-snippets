******

 */

$(document).ready(function() 
{
     var width = screen.width;
	 var height = screen.height;
	 
	 $("#my-screenres").html( "Your screen resolution is " + width + ' x ' + height + '.');
});
 